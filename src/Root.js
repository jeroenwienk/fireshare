import React, { Component } from "react";
import PropTypes from "prop-types";

class Root extends Component {
  static propTypes = {

  };


  render() {
    return (
      <div>Root</div>
    );
  }
}

export default Root;